# 数据目录

这里存放练习数据。

默认脚本会生成一个模拟光谱数据文件：

- `synthetic_spectra.csv`

字段说明：

- `sample_id`：样本编号。
- `label`：模拟类别，`normal` 或 `lesion`。
- `wl_400` 到 `wl_900`：不同波长处的模拟强度。

真实研究中，你需要额外记录：

- 样本来源。
- 采集设备。
- 采集参数。
- 标注依据。
- 是否同一患者有多个样本。
- 伦理和隐私合规信息。
